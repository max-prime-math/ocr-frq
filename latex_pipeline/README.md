# OCR-FRQ LaTeX

LaTeX-native OCR-FRQ pipeline that renders `.tex` and compiles with `pdflatex`.

Run:

```bash
python run_pdf.py --sg SG-BC-2018.pdf --exam BC-2018.pdf --force
```

Outputs default to `output_latex/`.
